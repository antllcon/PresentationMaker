// calc - task-1.3

function tokenizeString(input: string): Array<string> {
    const regex = /[^\s.,;!?]+/g;
    const tokens = input.match(regex);
    return tokens;
}

const input = "Hello, World!";
console.log(tokenizeString(input));
